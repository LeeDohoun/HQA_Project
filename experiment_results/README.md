# Experiment Results

This directory stores generated experiment outputs.

- `backtesting/`: backtest reports, comparison tables, and raw run artifacts.

The `data/` directory should stay focused on source data and derived datasets, not experiment result dumps.
